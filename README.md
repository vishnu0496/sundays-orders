# Sundays Order Tracker

Offline, single-file order + kitchen ops tool for the Sundays cookie business.
Open `index.html` in any browser. Data lives in the browser (localStorage); use Backup/Restore in the menu.

No build, no server, no dependencies.
